import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: { flex: 1, alignItems: "center" },
  title: {
    textAlign: "center",
    fontWeight: "bold",
    fontSize: 32,
    marginTop: 60,
  },
  image: { width: 300, height: 300, resizeMode: "contain", marginTop: 50 },
});

export default styles;
